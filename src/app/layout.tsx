import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ICHRA Legislation Watch — Track ICHRA Bills Across All 50 States",
  description:
    "An independent, open resource tracking ICHRA legislation at the state and federal level. Monitor tax credit bills, legislative progress, and policy updates.",
  openGraph: {
    title: "ICHRA Legislation Watch",
    description:
      "Track ICHRA legislation across all 50 states and at the federal level.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
